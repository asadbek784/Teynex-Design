import Link from 'next/link'
import { ArrowUpRight, Bot, Boxes, ChartNoAxesCombined, Code2, FileText, FolderKanban, Gauge, Globe2, LayoutDashboard, MessageSquare, PanelLeft, Play, Plus, Search, Settings2, Sparkles, Terminal, Users } from 'lucide-react'
import { FlowNav, ScreenHeader, Shell } from '@/components/design-system'
import { screens } from '@/components/screen-data'

const iconMap = { 'ai-builder': Bot, dashboard: LayoutDashboard, blocks: Boxes, 'ui-kit': Code2, openpage: Globe2 }
const palettes = { 'ai-builder': ['bg-primary', 'bg-chart-2', 'bg-chart-4'], dashboard: ['bg-chart-3', 'bg-primary', 'bg-chart-5'], blocks: ['bg-chart-1', 'bg-chart-3', 'bg-primary'], 'ui-kit': ['bg-chart-4', 'bg-chart-2', 'bg-chart-5'], openpage: ['bg-chart-2', 'bg-chart-1', 'bg-chart-4'] }

export function DesignScreen({ slug }: { slug: keyof typeof iconMap }) {
 const screen = screens.find((item) => item.slug === slug)!
 const Icon = iconMap[slug]
 const palette = palettes[slug]
 return <Shell><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:py-20"><ScreenHeader screen={screen} /><div className="grid gap-6 lg:grid-cols-[1fr_0.38fr]"><section className="overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div className="flex items-center gap-3"><Icon className="size-4 text-primary" /><span className="font-mono text-xs">{screen.label.toLowerCase().replace(' ', '-')} / workspace</span></div><div className="flex gap-1"><span className="size-2 rounded-full bg-destructive" /><span className="size-2 rounded-full bg-chart-2" /><span className="size-2 rounded-full bg-primary" /></div></div><div className="grid min-h-[420px] grid-cols-[180px_1fr] sm:grid-cols-[220px_1fr]"><aside className="hidden border-r border-border p-4 sm:block"><div className="mb-8 flex items-center gap-2 font-mono text-xs"><span className="flex size-6 items-center justify-center rounded-md bg-primary text-primary-foreground"><Sparkles className="size-3" /></span> studio</div><div className="space-y-1">{['Overview', 'Projects', 'Components', 'Activity', 'Settings'].map((item, i) => <div key={item} className={`flex items-center gap-2 rounded-md px-3 py-2 text-xs ${i === 0 ? 'bg-accent text-foreground' : 'text-muted-foreground'}`}><PanelLeft className="size-3" />{item}</div>)}</div></aside><div className="min-w-0 p-5 sm:p-8"><div className="mb-8 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Workspace / recent</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.04em]">{slug === 'ai-builder' ? 'What will you build?' : slug === 'dashboard' ? 'Good morning, Alex.' : slug === 'blocks' ? 'Choose a starting point.' : slug === 'ui-kit' ? 'A system with intent.' : 'Make the work visible.'}</h2></div><button className="rounded-lg bg-primary p-2 text-primary-foreground" aria-label="Add new"><Plus className="size-4" /></button></div><div className="mb-6 flex items-center gap-3 rounded-lg border border-border bg-background px-3 py-3 text-sm text-muted-foreground"><Search className="size-4" /> Search your workspace...</div><div className="grid gap-3 sm:grid-cols-3">{['Signal', 'Orbit', 'Northstar'].map((label, i) => <div key={label} className="rounded-xl border border-border p-4"><div className={`mb-8 h-16 rounded-lg ${palette[i]} opacity-80`} /><p className="text-sm font-medium">{label}</p><p className="mt-1 font-mono text-[10px] text-muted-foreground">Updated just now</p></div>)}</div><div className="mt-6 rounded-xl border border-border p-4"><div className="mb-4 flex items-center justify-between"><span className="text-sm font-medium">Activity overview</span><ChartNoAxesCombined className="size-4 text-muted-foreground" /></div><div className="flex h-20 items-end gap-2">{[30, 54, 42, 78, 60, 88, 70, 96, 82, 100, 74, 92].map((height, i) => <div key={i} className="flex-1 rounded-t bg-primary/70" style={{ height: `${height}%` }} />)}</div></div></div></div></section><aside className="space-y-4"><div className="rounded-2xl border border-border bg-card p-6"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Screen notes</p><div className="mt-5 space-y-4 text-sm leading-6 text-muted-foreground"><p><span className="text-foreground">Purpose.</span> This screen translates the source reference into a reusable, production-ready surface.</p><p><span className="text-foreground">Pattern.</span> Sidebar navigation, focused content canvas, and compact action hierarchy.</p></div></div><div className="rounded-2xl border border-primary/30 bg-primary/5 p-6"><div className="flex items-center gap-2 text-primary"><Gauge className="size-4" /><span className="font-mono text-[10px] uppercase tracking-widest">System status</span></div><p className="mt-4 text-sm leading-6 text-muted-foreground">All shared tokens, responsive states, and navigation links are connected.</p><div className="mt-5 h-1 overflow-hidden rounded-full bg-primary/15"><div className="h-full w-full rounded-full bg-primary" /></div></div></aside></div><FlowNav current={slug} /></main></Shell>
}
